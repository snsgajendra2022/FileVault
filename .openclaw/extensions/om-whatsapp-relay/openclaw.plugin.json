{
  "id": "om-whatsapp-relay",
  "name": "OM WhatsApp Relay",
  "description": "WhatsApp → Filevault dev server (9093): instant image upload to /api/images/upload + OM tools (bypasses broken embedded agent).",
  "activation": {
    "onStartup": true
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "relayUrl": {
        "type": "string",
        "description": "POST endpoint that returns { reply: string }"
      }
    }
  }
}
